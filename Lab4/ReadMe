Homework:
  LocationProcessor:
	  am creat o mapare care retine timpul dintre locatii
	  createGraph : metoda pentru a crea un graf bazat pe locatiile si timpul dinte ele
	  calculateShortestTimes : metoda pentru a calcula timpul minim dintre locatii cu ajutorul algoritmului lui Dijkstra
	  printShortestTimes : metoda pentru a afisa locatiile grupate dupa tipuri

  MapGraph:
	  Constructorul creaza un graf ponderat
	  addEdge : adauga muchii intre locatii cu timpul asociat (setam timpul ca si greutate a muchiei)
	  indFastestRoutes : cauta cele mai rapide drumuri folosind algoritmul lui Dijkstra (drum de cost minim)

Bonus:
SafeRouteCalculator:
	Calculam cele mai sigure rute pe baza probabilitatilor
	calculateSafestRoutes: afisează rutele si probabilitatea de siguranta intre locatii
	printSafeRoutes: afiseaza doar rutele care au o probabilitate de siguranta mai mare de 0.8

RandomProblemGenerator:
	generateRandomLocation: creeaza o locatie cu un nume aleatoriu si o probabilitate de siguranta random (intre 0 si 1)
	generateRandomLocations: creeaza o lista de locatii aleatorii

StatisticsCalculator:
	calculateStatistics: afiseaza numarul de locatii de fiecare tip
